server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Esto es lo más importante: redirige todo al index.html
        try_files $uri $uri/ /index.html;
    }

    # Opcional: Optimización para archivos estáticos
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}